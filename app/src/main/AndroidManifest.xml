<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.temple.palette">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/palette_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/palette_icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".CanvasActivity" android:label="Canvas Activity"></activity>
        <activity android:name=".PaletteActivity" android:label="Palette">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>